
name: telegram-deals-bot

build:
  dockerfile: Dockerfile

run:
  cmd: python main.py
  env:
    - TELEGRAM_TOKEN
    - AMAZON_ASSOCIATE_TAG
    - AWS_ACCESS_KEY
    - AWS_SECRET_KEY
    - MONGO_URI
    - CHANNEL_ID
